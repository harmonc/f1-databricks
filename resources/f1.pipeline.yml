# The main pipeline for f1
resources:
  pipelines:
    f1_pipeline:
      name: f1_pipeline
      catalog: workspace
      schema: f1_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
